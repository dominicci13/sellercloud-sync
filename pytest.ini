[pytest]
# pytest-html 2.0.1 and seleniumbase 4.30.3 import pkg_resources at plugin
# load; neither is used here, so keep them out of the test session.
addopts = -p no:html -p no:seleniumbase
testpaths = tests
pythonpath = .
